# RepoMetadata — CBA repo governance manifest.
#
# ⚠️ TEMPLATE — values below are placeholders. The exact schema and required
# field values are defined in CBA-General/ai-engineering (enterprise GitHub host,
# not reachable from this session). Before pushing to CBA-General/PRep, copy the
# canonical RepoMetadata.yaml from that repo and fill in the real values, or the
# org-required metadata CI check will fail.
#
# The keys below reflect the common CBA RepoMetadata shape; reconcile names/values
# with ai-engineering.

apiVersion: v1
kind: RepoMetadata

metadata:
  name: PRep
  description: >-
    Chrome (MV3) extension that quizzes engineers on pull-request changes,
    powered by the Portkey AI gateway (Claude on Bedrock).
  # EIM / asset identifier registered for this repo. REQUIRED — get from ai-engineering.
  eimId: "REPLACE_ME"

spec:
  # Owning crew / team and tech lead. REQUIRED.
  owner: "REPLACE_ME"
  crew: "REPLACE_ME"
  # one of the org's allowed values (e.g. experimental | production | deprecated)
  lifecycle: experimental
  # one of the org's allowed values (e.g. public | internal | confidential)
  dataClassification: internal
  language: typescript
  buildTool: bun
  repository: https://github.com/CBA-General/PRep
